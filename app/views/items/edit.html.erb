<%= link_to 'Back', location_path(@location) %>

<%= render 'form',
  location: @location,
  item: @item,
  action_label: "Save" %>

<hr/>

<%= button_to "Delete", location_item_path(@location, @item), data: { confirm: "Are you sure?" }, method: :delete, class: "m--danger" %>
